import React from "react";
import { Grid } from "@mui/material";
import './SideMenu.css';

export const SideMenu = () => {
    return(
        <Grid className='sideMenuContainer' container item xl={2} xs={1}>

        </Grid>
    );
}